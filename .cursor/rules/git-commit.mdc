---
description: Git 提交规范（中文 commit message）
alwaysApply: true
---

# Git 提交规范

创建 git commit 时，**提交说明必须使用中文**。

## 格式

- **标题**：一句中文概括「为什么改」，≤ 50 字，不用英文标题
- **正文**（可选）：1～3 句中文，说明背景、影响范围或注意事项
- 不要使用 `feat:`、`fix:` 等英文 Conventional Commits 前缀，除非用户明确要求

## 示例

```
增强对话页体验：输出风格、悬浮会话按钮与可拖动工具栏

新增可选对话输出风格；新建对话与历史会话移至对话区右下角；底部工具栏支持拖动排序；提交前自动更新 README。
```

## 提交前检查

1. 运行 `npm run update-readme`，刷新 README「更新记录」（展示为中文）
2. 若有新功能，同步更新 README 功能说明
3. 将 `README.md` 纳入本次 commit（pre-commit 钩子会自动刷新更新记录）

## README 更新记录

- 更新记录条目一律使用**中文**说明
- 新提交已是中文时，脚本直接采用 commit 标题
- 历史英文提交需在 `scripts/commit-message-zh.json` 中按短 hash 补充中文摘要
